<%= simple_form_for([ product, review ]) do |form| %>

  <%= form.input :rating, collection: [1, 2, 3, 4, 5]%>
  <%= form.input :content, as: :text %>
  <%= form.button :submit, class: "btn btn-primary" %>
<% end %>
